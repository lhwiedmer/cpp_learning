#ifndef RETANGULO
#define RETANGULO

class Retangulo{
	public:
		unsigned long calculaArea(unsigned long h, unsigned long w);
		unsigned long calculaPerimetro(unsigned long h, unsigned long w);
		unsigned long h;
		unsigned long w;

};

#endif