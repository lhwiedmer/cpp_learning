#include "Retangulo.hpp"

unsigned long Retangulo::calculaArea(unsigned long h, unsigned long w) {
	return h*w;
}

unsigned long Retangulo::calculaPerimetro(unsigned long h, unsigned long w) {
	return h+w;
}